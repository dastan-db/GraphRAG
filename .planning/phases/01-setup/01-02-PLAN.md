<task type="auto">
<name>Ingestion pipeline</name>
<files>graphrag/ingest/models.py graphrag/ingest/parser.py graphrag/ingest/pipeline.py graphrag/graph/schema.py graphrag/graph/sync.py</files>
<action>
Build Tree-sitter based Python parser that extracts entities (modules, classes,
functions) and relationships (imports, calls, inheritance, contains). Create
ingestion pipeline that walks a directory, parses all .py files, and syncs
results to Neo4j. Define Neo4j schema with node labels and relationship types.
</action>
<verify>python -c "from graphrag.ingest.pipeline import IngestPipeline; p = IngestPipeline.from_config(...); p.run('tests/fixtures/sample_fastapi')"</verify>
<done>Parser extracts correct entities/relationships from sample fixture; Neo4j contains the knowledge graph.</done>
</task>
