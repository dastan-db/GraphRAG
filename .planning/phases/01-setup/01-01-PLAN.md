<task type="auto">
<name>Project scaffold</name>
<files>pyproject.toml docker-compose.yml graphrag/__init__.py graphrag/config.py graphrag/ingest/__init__.py graphrag/query/__init__.py graphrag/graph/__init__.py tests/fixtures/sample_fastapi/</files>
<action>
Create Python package structure with pyproject.toml, Docker Compose for Neo4j,
config module with Pydantic models, and a sample FastAPI order-service fixture
matching the blog's architecture (AuthService, OrderService, PaymentService with
routers, services, and stub dependencies).
</action>
<verify>ls -R graphrag/ tests/fixtures/sample_fastapi/ && python -c "from graphrag.config import GraphRAGConfig; print(GraphRAGConfig())"</verify>
<done>Package structure exists with all sub-modules; config loads without errors; sample fixture has realistic code.</done>
</task>
