<task type="auto">
<name>Query pipeline and demo</name>
<files>graphrag/query/chain.py graphrag/query/prompts.py notebooks/demo.ipynb</files>
<action>
Create LangChain GraphCypherQAChain setup with custom prompt templates tuned for
codebase graph schema. Build end-to-end demo notebook that ingests the sample
FastAPI codebase and runs architectural queries.
</action>
<verify>Run demo notebook end-to-end; verify correct answers for "If we refactor AuthService, what breaks?" and "Trace the order flow".</verify>
<done>Query pipeline returns correct, grounded answers; demo notebook runs end-to-end.</done>
</task>
