bundle:
  name: graphrag-solution-accelerator

include:
  - deploy/*.yml

variables:
  catalog:
    default: "serverless_8e8gyh_catalog"
  schema:
    default: "graphrag_bible"
  warehouse_id:
    lookup:
      warehouse: "Serverless Starter Warehouse"
  enron_schema:
    default: "graphrag_enron"
  lakebase_project:
    default: "graphrag"

targets:
  dev:
    default: true
    mode: development
    workspace:
      profile: DEFAULT
    variables:
      catalog: "serverless_8e8gyh_catalog"
      schema: "graphrag_bible"
      enron_schema: "graphrag_enron"

  prod:
    mode: production
    workspace:
      profile: DEFAULT
    variables:
      catalog: "serverless_8e8gyh_catalog"
      schema: "graphrag_bible"
      enron_schema: "graphrag_enron"
